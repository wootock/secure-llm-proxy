startCommand:
  type: http
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        description: Your OpenAI API key. The proxy strips PII before forwarding.
    required:
      - apiKey
  exampleConfig:
    apiKey: sk-proj-0450de270edd...
serverUrl: https://nexus.exchange-secure.com/mcp
